import todosServices from "../todos.service.js";
import { expect, describe, it, jest } from "@jest/globals";

describe("Todos Service", () => {
    describe("getTodos", () => {
        it("should return an array of todos", async () => {
        });
    });
});